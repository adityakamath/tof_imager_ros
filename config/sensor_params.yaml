tof_imager:
    ros__parameters:
        frame_id: tof_frame
        resolution: 4 # [4 (4x4), 8 (8x8)]
        mode: 1 # [1 (continuous), 3 (autonomous)]
        ranging_freq: 60 # limited to 15Hz if resolution is 8x8, else 60Hz
        timer_period: 0.1
